import { MobileFormatDirective } from './mobile-format.directive';

describe('MobileFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileFormatDirective();
    expect(directive).toBeTruthy();
  });
});
